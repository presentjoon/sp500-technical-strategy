# S&P 500 기술적 지표 전략 — 연구 프로젝트

**연구 질문:** RSI·볼린저밴드 신호는 무작위 진입보다 나은 수익을 내는가?
그리고 아니라면, 왜 아닌가?

세종대 최준연 교수님 지도 과제 (랩실 진입 평가). 1단계 기간 2026-08-02 ~ 08-16.
1단계는 모든 것을 직접 구현한다. 2단계에서 Backtrader / vectorbt 결과와 대조하며,
차이가 나면 (a) 내 코드 버그 (b) 프레임워크의 다른 기본 가정 (c) 반올림 오차 중
어느 쪽인지 규명하는 것이 리포트의 핵심 재료다.

## 협업자 정보

- AI학과 2학년. **파이썬 문법이 초급 수준이다** (슬라이싱, 메서드 체이닝에서 막힘).
- 금융·퀀트 도메인은 입문 단계.
- **이해하지 못한 코드는 이 레포에 들어가면 안 된다.** 랩실 평가에서 무너진다.
  코드를 작성하면 반드시 설명을 함께 제공한다. 설명 없이 커밋하지 않는다.
- 새로운 개념이 나오면 `docs/glossary.md`에 한국어 정의 + 영어 병기로 누적한다.

## 방법론상의 절대 규칙

위반하면 결과 전체가 무효가 된다. 성능이나 편의보다 우선한다.

1. **미래 참조(lookahead bias) 금지.** 신호는 t일 종가로 확정, 진입은 t+1일.
   `shift(1)`이 들어가는 모든 위치에 `# LOOKAHEAD GUARD` 주석을 단다.
2. **무작위 벤치마크 없는 신호 평가 금지.** S&P 500은 표본 기간 내내 우상향했으므로
   아무 날이나 사도 양의 수익이 난다. 신호의 가치는 절대 수익률이 아니라
   **무작위 진입 대비 초과분**으로만 판단한다. 순열검정으로 p-value를 산출한다.
3. **결측치를 보간하지 않는다.** 보간은 존재한 적 없는 가격을 만드는 행위이고,
   그 가격에서 나온 신호는 사실이 아니다. 버리고, 무엇을 버렸는지 `reports/`에 기록한다.
4. **단일 구간 성과를 믿지 않는다.** `config.MARKET_REGIMES`의 6국면 전체에서 검증한다.
   단 국면 분류는 사후적이므로, 매매 규칙에 쓰는 순간 미래 참조가 된다.
5. **벤치마크는 `^SP500TR`.** `^GSPC`는 가격지수라 배당이 빠져 있어, 벤치마크로 쓰면
   매년 약 1.8%p씩 상대를 봐주게 된다. `^GSPC`는 OHLC가 있으므로 신호 생성 전용.
   현금 보유 구간의 무위험수익률도 빼먹지 않는다 (2000~2007년 단기금리는 4~5%였다).

## 재현성

- 라이브러리 기본값에 의존하지 않는다. `auto_adjust` 등은 항상 명시적으로 지정한다.
- 수정주가는 오늘 기준으로 매번 재계산되므로, 원본을 parquet 스냅샷으로 굳혀둔다.
- 난수를 쓰는 모든 곳(순열검정, 부트스트랩)에 `config.RANDOM_SEED`를 넘긴다.
- 백테스트 가정(수수료·슬리피지·체결시점·초기자본)은 **전부 `src/config.py`에만** 존재한다.
  다른 파일에 숫자를 하드코딩하지 않는다. 2단계에서 Backtrader로 그대로 옮길 값들이다.

## 코드 규약

- **메서드 체이닝 금지.** 한 줄에 한 연산, 중간 결과에 이름을 붙인다.
  compact한 코드보다 읽기 쉬운 코드를 항상 우선한다.
- **각 줄 끝에 결과 타입과 shape 주석.** 예: `# -> DataFrame (6935, 2)`
- 모든 함수는 **티커가 여러 개일 수 있다고 가정**한다. `groupby("ticker")` 기준으로 쓴다.
  지수는 티커 1개인 특수 케이스일 뿐이다. 티커 경계를 넘는 `pct_change()`는
  존재한 적 없는 수익률을 만든다 — long format에서 가장 흔한 버그다.
- 데이터는 **long format** `(date, ticker, ...)`. 나중에 뉴스 데이터를
  `(date, ticker, headline)`으로 조인하기 위한 설계다.
- 로직은 `src/`, 실행·탐색은 `notebooks/`. 노트북에 로직을 넣으면 2단계에서
  "같은 신호를 다른 엔진에 넣기" 실험 자체가 불가능해진다.
- 저장은 parquet.

## 결과를 의심해야 하는 신호

낙관적 결과가 나오면 축하가 아니라 검증을 제안한다.

- 샤프 비율이 2를 넘으면 미래 참조부터 의심한다.
- 파라미터 히트맵이 뾰족한 봉우리 하나면 과최적화다. 넓고 완만한 고원이어야 한다.
- 신호가 특정 시기(금융위기 등)에 몰려 있으면 전략이 아니라 위기 탐지기다.
- 파라미터·조합 탐색 횟수가 늘어나면 다중검정 문제를 상기시킨다.
- 긴 표본은 공짜가 아니다. 2001년 호가 십진화, HFT 확산, ETF 자금 유입으로
  시장 미시구조가 변했다. 2003년의 패턴이 지금도 유효하다는 보장은 없다.

## 산출물

- **`reports/why_it_fails.md`가 이 프로젝트의 하이라이트다.** "왜 실패하는가"를
  성과만큼 중요하게 다룬다.
- 매일 마지막 30분: 커밋 + `logs/YYYY-MM-DD.md`에 3줄 (한 것 / 막힌 것 / 내일 첫 작업).
- **같은 시점에 `docs/daily_workflow.md`에 그날 절을 추가한다.** 형식과 작성 원칙은
  그 문서 맨 아래 "갱신 규칙"에 있다. `logs/`가 "무엇을 했는가"의 원본 기록이라면,
  `daily_workflow.md`는 **"그게 왜 중요한가"를 처음 보는 사람에게 설명하는** 문서다.
  둘의 역할이 다르므로 한쪽으로 합치지 않는다. 수치는 `reports/week2_key_numbers.md`
  (정본)에서 가져오고, "무슨 의미인가" 절을 비워둔 채 커밋하지 않는다.
- Notion 갱신은 **자동이다.** `docs/daily_workflow.md`가 바뀐 커밋에서
  `scripts/hooks/post-commit`이 `scripts/push_notion.py`를 백그라운드로 돌린다.
  따로 실행하지 않는다. 결과는 `logs/notion_push.log`에서 확인한다.
  대상은 PROJECT 아래 `일일 워크플로우` 페이지 **하나뿐**이며, 스크립트가 기존
  블록을 지우고 시작하므로 다른 페이지를 대상으로 지정하지 않는다.
- `python scripts/export_notion.py`로 만드는 `docs/notion_project.md`는
  붙여넣기용 대비책이다. 훅이나 API가 막혔을 때만 쓰고, 평소에는 갱신하지 않아도 된다.
- **Notion 페이지와 `docs/notion_project.md`는 둘 다 생성물이라 직접 편집하지 않는다.**
  다음 실행에 덮어써지고, 손으로 고치는 순간 정본이 둘이 된다.
- 커밋 메시지는 무엇을 왜 바꿨는지 영어로 한 문장. `update`, `수정` 같은 메시지는 쓰지 않는다.
  실패와 수정의 흔적이 남은 히스토리가 완벽한 결과만 올라온 레포보다 신뢰를 준다.
- 그림은 `figures/`에 png로 저장하고 리포트에서 참조한다.
- 노트북 출력은 커밋한다(GitHub에서 결과가 바로 보여야 함).
  단 노트북 파일이 **1MB**를 초과하면 정책 재검토.
  용량은 출력을 지워서가 아니라 **그림 자체를 줄여서** 해결한다
  (`config.FIGURE_DPI`, `bbox_inches="tight"`).
  - 2026-08-04 상향(500K → 1MB): GitHub은 1MB대 노트북도 정상 렌더링하므로,
    그림 인라인 출력을 유지하는 편이 산출물 가시성 측면에서 유리하다고 판단해
    상향. plt.close()로 출력을 억제하면 GitHub에서 노트북을 열었을 때 그림이
    보이지 않아, 산출물을 보여주려고 만든 노트북에서 산출물을 지우는 셈이 된다.
- 모든 `savefig()`에 `dpi=config.FIGURE_DPI`와 `bbox_inches=config.FIGURE_BBOX`를
  명시한다. matplotlib 기본값에 의존하지 않는다.

## 검증 명령

코드를 수정한 뒤 아래가 통과해야 한다.

```bash
python -c "from src import config; print(config.TICKERS)"
python scripts/day01_collect.py     # 수집 + audit 리포트 생성
```

`audit()` 결과에서 (date, ticker) 중복은 반드시 0, OHLC 정합성 위반도 0,
티커별 연평균 행 수는 252 근처여야 한다.