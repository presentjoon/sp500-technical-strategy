# D7 강건성 검증 — 검정통계량 선택과 이상치 민감도

**작성일**: 2026-08-08 (D7)
**대상**: `docs/signal_spec.md` §6.5 순열검정 20조합, 특히 S1 h=1
**상태**: Phase 0 — 사전 약속 기록 완료, 검증 미실행

---

## 1. 문서의 지위

**이 문서는 사후 탐색적(post-hoc exploratory) 강건성 검증이다. 확증 검정이 아니다.**

| 항목 | 내용 |
|---|---|
| 확증 판정 | **원안 유지.** S1 h=1, 평균 통계량, $B = 10{,}000$, 시드 42, $p = 0.0019$, Bonferroni 통과 |
| 사전등록 검정통계량 | **평균.** §6.5에서 "중앙값이나 t통계량으로 바꾸지 않는다"고 명시 |
| 검정 family | **불변.** $m = 20$, $\alpha_{adj} = 0.0025$ |
| 이 문서의 값이 쓰이는 곳 | "이 유의성을 어느 정도 신뢰할 것인가". D14 `why_it_fails.md`의 재료 |

**Phase 1의 스튜던트화 통계량은 사전등록 통계량의 교체가 아니라 강건성 점검이다.**
결과가 어떻게 나오든 확증 판정을 수정하지 않으며, `docs/signal_spec.md` §4~§7에
어떤 수치도 반영하지 않는다. §8 한계 항목에 포인터 한 줄만 추가한다.

---

## 2. 사전 약속문

> **본 검증은 결과와 무관하게 전량 기록한다. S1 h=1의 확증 판정은 원안(평균
> 통계량, $B = 10{,}000$, 시드 42, $p = 0.0019$, Bonferroni 통과)을 유지하며,
> 스튜던트화 결과에 따라 판정·사전등록 통계량·검정 family를 수정하지 않는다.
> 1회만 실행한다.**

이 문단은 검증 실행 **전에** 작성해 커밋했다. 결과를 본 뒤 판독 기준을 옮기거나
유리한 통계량을 골라잡는 것을 막기 위한 장치이며, 커밋 시각이 그 증거다.

---

## 3. 배경 — D6가 남긴 질문

D6 사후 진단(`reports/day06_diagnostics.md`)이 확인한 것:

| 항목 | 값 |
|---|---|
| Bonferroni 통과 | 1건 (S1 h=1, $p = 0.0019$) |
| 그 조합의 `sd_ratio` | **2.144** — 20조합 중 최고 |
| `t_naive` → `t_adj` | −3.34 → **−1.56** (분산 이질성 반영 시 절반 이하) |
| 효과 집중도 | 3개 관측이 **86.1%** |
| 대안 2 (국면 편중) | **기각** (rate_ratio 2.61 < 기준 3) |
| 대안 1 (분산 불일치) | **지지** |

**미해결로 남은 질문: "그러면 분산 이질성을 제대로 반영한 검정에서는 어떻게
되는가."** D7 오전에 이것을 닫는다.

### 왜 표준 순열검정이 이 상황에서 기각하는가

표준 순열검정은 **날카로운 귀무가설(sharp null)** — 라벨과 수익률이 **완전히
독립** — 을 검정한다. 이 가설 하에서는 평균뿐 아니라 분포 전체가 같아야 하므로,
**평균이 같아도 분산이 다르면 기각된다.**

우리가 알고 싶은 것은 그것이 아니라 **약한 귀무(weak null)** — 평균만 같음 —
이다. 검정통계량을 **스튜던트화(studentize)** 하면 약한 귀무 하에서도 점근적으로
타당해진다. Janssen (1997), Chung & Romano (2013).

---

## 4. Phase 1 — 스튜던트화 순열검정 (계획)

### 검정통계량

$$
T = \frac{\bar{r}_{\text{sig}} - \bar{r}_{\text{pool}}}{s_{\text{sig}} / \sqrt{n}}
$$

**각 순열 표본 $b$에 대해 그 표본 자신의 표준편차 $s_b$로 스튜던트화한다.**
$s$를 관측표본 값으로 고정하면 분모가 상수가 되어 스튜던트화의 의미가 사라지고,
결국 평균 통계량과 같은 검정이 된다.

| 항목 | 값 |
|---|---|
| $s$ 계산 | `ddof=1` 명시 |
| $p$-value | $(\text{count} + 1) / (B + 1)$ 보정 유지 |
| $B$ | 10,000 (사전등록과 동일) |
| 시드 | 42 (사전등록과 동일) |
| 방향 | 양측 (사전등록과 동일) |

**대상: 20조합 전부.** S1 h=1만 돌리지 않는다. 20개를 다 돌려야 "통계량을 바꾸면
판정이 어떻게 달라지는가"의 전체 그림이 나오고, 통과 조합만 골라 다시 보는
선택 편향을 피할 수 있다.

산출 컬럼: `signal_id, h, n, sd_ratio, p_perm_mean, p_perm_stud, 판정_mean, 판정_stud`

- 저장: `reports/day07_studentized.csv`
- 그림: `figures/day07_p_mean_vs_stud.png` — 산점도, x = `p_perm_mean`,
  y = `p_perm_stud`, 색으로 `sd_ratio` 표시, $\alpha_{adj}$ 기준선 2개

### 판독 기준 (실행 전 확정)

| 조건 | 판단 |
|---|---|
| 스튜던트화 $p > \alpha_{adj} = 0.0025$ | 원안 유의성이 **분산 이질성에 기인했을 가능성이 크다** |
| 스튜던트화 $p > \alpha = 0.05$ | **평균 효과의 증거로 해석하지 않는다** |

---

## 5. Phase 2 — 강건성 재계산 3건 (계획)

### 2-1. 3일 제거 후 재계산 (S1 h=1)

제거 대상: **2020-03-12, 2008-10-07, 2011-08-04** (h=1 수익률 최악 3일)

$n = 62$ 기준으로 `mean_sig`, `sd_sig`, `sd_ratio`, `t_adj` 재산출.

**목적은 보정이 아니라 대비다.** 평균은 −0.502 → −0.101%p로 5배 축소가 이미
확인됐다. **분산비가 같은 정도로 무너지는지, 아니면 유지되는지**를 본다.
두 통계량의 강건성 차이가 D14의 재료다.

**판독 기준 (실행 전 확정):** 3일 제거 후 `sd_ratio`가 **1.5 이상 유지되면**
분산비를 **이상치에 강건한 발견**으로 취급한다.

### 2-2. 86.1%의 분모 명시

D6 문서의 "3일이 86.1%" 진술에 대해 아래 두 값을 **모두** 산출하고, **어느 분모를
쓴 것인지 명시한다.**

| 분모 후보 | 정의 |
|---|---|
| 음수 관측 합 | 65개 중 음수인 관측만의 합 |
| 초과수익 총합 | 65개 편차 $(r_i - \bar{r}_{\text{pool}})$ 의 합 |

재현 가능하도록 **두 값과 각 분모의 절대값을 함께 적는다.**

### 2-3. 분산비 VR(20)

$$
VR(20) = \frac{\mathrm{Var}(20\text{일 수익률})}{20 \times \mathrm{Var}(1\text{일 수익률})}
$$

무조건부 전체 표본 기준. **신호와 무관한 시장 자체의 성질이므로 $m = 20$ family와
무관하며 다중검정 문제가 없다.**

- 등분산 iid 귀무 하 표준오차와 $z$값 산출
- **한계를 반드시 병기:** 팻테일·이분산 하에서 등분산 SE는 **과소 추정**이며,
  중첩 창으로 유효 표본이 $6684 / 20 \approx 334$로 감소한다. 따라서 **$z$값은
  방향 참고용이며 유의성 주장에 쓰지 않는다.**
- **이분산 강건 버전(Lo–MacKinlay 등)은 구현하지 않는다.** D14 후보로 기록만 한다.

---

## 6. 이월 항목 (오늘 하지 않는 것)

| 항목 | 사유 | 이월처 |
|---|---|---|
| 층화 순열 | 새로운 검정이라 $m$ 문제가 걸린다 | D12 |
| 이분산 강건 VR | 구현 범위 초과 | D14 후보 |
| 신호 조합 실험 | **family를 흔들지 않기 위해 D7 원계획에서 제외** | 미정 |
| 진입 지연·임계값 변형 | 진입 시점은 확증 대상에 가까워 $m$ 재계산 필요 | D14 가설 목록 |

---

## 7. Phase 1 결과

(실행 후 기입)

## 8. Phase 2 결과

(실행 후 기입)

## 9. 종합 판단

(실행 후 기입)
